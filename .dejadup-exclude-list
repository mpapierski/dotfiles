#
# Use ~/.local/bin/reload-dejadup-config to reload gsettings
# 
# Author: Michał Papierski <michal@papierski.net>
#

# Its already synced so no need
~/Dropbox

# Custom apps
~/snap

~/.local/share/Trash
~/.cache
~/.npm
~/.cargo
~/.rustup
~/.virtualenvs
~/.dropbox-dist
~/.dropbox
~/.sbt
~/.fzf
~/.zoom
~/.ivy2

# Its already in dotfiles, so no need to keep track of it
~/.vim
~/.viminfo
~/.dotfiles
~/.pyenv
~/.nvm
~/.oh-my-zsh

# Temporary data that always changes

~/.config/**/Cache
~/.config/**/CachedData

# Do not backup /target/ for Rust projects
~/Projects/**/target

# Conan data
~/.conan/data

# Virtual Machines
~/vmware
